[pytest]
norecursedirs = .git .venv venv build deps tests/deps
addopts = -v --tb=long
    --splunk-type=docker
    --splunk-host=splunk
    -n 15
    --keepalive
filterwarnings =
    ignore::DeprecationWarning
